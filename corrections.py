"""corrections.py — SQLite learning layer: corrections + vocab.

Two tables (spec.md "Learning layer"):
  corrections(id, wrong_text, correct_text, hit_count, created_at)
  vocab(id, term, created_at)

Filled in during Phase 6.
"""
