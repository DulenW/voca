"""main.py — the rumps menu bar app. Wires everything together.

Loads the Whisper model once at start (keeps it warm), registers the hotkey
listener, and manages the ready/recording/transcribing states.
Filled in during Phase 5.
"""
