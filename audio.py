"""audio.py — microphone capture (16kHz mono float32) via sounddevice.

Filled in during Phase 2. The mic stream is opened ONLY while the hotkey is
held and closed immediately on release (battery constraint, spec.md).
"""
