"""config.py — load and save the JSON config from the app support directory.

Filled in during Phase 5 (menu bar app). Kept as its own module so settings
have one home. See spec.md "Config file".
"""
